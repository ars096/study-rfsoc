# 試行インデックス

`projNNN` を1行ずつ記録する。**新しい proj を作ったらここに追記する。**
番号が3桁ゼロ詰めなのは、辞書順でそのまま並ぶようにするため。

| proj | 日付 | 目的 | 結果 |
|---|---|---|---|
| [proj001](proj001/) | 2026-09-11 | 開発環境の再構築と生存確認。PS を使わない最小構成（自走 100 MHz → 27bit カウンタ → ユーザ LED）| **成功**。LED 点滅を確認。ツールチェーン・ライセンス・ケーブル・ボード・ピン定義を同時に検証 |
| [proj002](proj002/) | 2026-09-15 | PS ↔ PL の疎通と PYNQ オーバーレイの流儀。Zynq MPSoC + AXI GPIO で LED を叩く。Vivado 2024.1 へ移行 | **成功**。PYNQ v3.1.1 からオーバーレイを読み込み LED を制御。board file / block design / `.hwh` の経路を実証 |
| [proj003](proj003/) | 2026-09-16 | RF Data Converter を置き、ADC の生サンプルを AXI DMA で PS へ吸い上げる。既知の CW トーンで周波数・振幅・折返しを検証する | **成功**。WNS +1.014 ns。ADC_B の CW を PYNQ 経由で取得し、第 1 / 第 2 ゾーンで ppm が一致（+14.92 / +14.91）。fs = 1228.8 MSPS を確定。無入力で LMX / LMK の回り込みも検出|
| [proj004](proj004/) | 2026-09-17 | 外部 10 MHz 基準クロック。LMK04828 の PLL1 の基準を基板の Si5395 から CLK_IN の SMA へ切り替え、proj003 の +14.9 ppm が消えることで裏を取る | **成功**。WNS +1.014 ns。**CLK_IN = CLKin0 を実測で確定**し、+14.912 → **−0.015 ppm**（約 1000 倍）。基準断のとき `PLLLockStatus` は 2 のまま 90 ppm ずれることも実証 |
| [proj005](proj005/) | 2026-09-17 | 正弦波源（Agilent E8257D）に替えて、「信号源のもの」と「ADC のもの」を分離する。proj004 で残したノイズフロアの 1.3 dB と、proj003 の方形波の件を決着させる | **成功**（SFDR は持ち越し）。**ノイズフロアの悪化は APSYN420 由来**と確定。H3 が −9.3 → −70.8 dBc で方形波を直接確認。ADC の圧縮なし。**0 dBFS ≒ +5.8 dBm** を実測。SFDR は SG の H2（−51.5 dBc）が壁でフィルタ待ち |
| [proj006](proj006/) | 2026-09-17 | 4ch 同時取得。ADC_A/B/C/D を 512 bit の 1 本に束ねて同じビートで取得し、SMA ラベルとタイル/スライスの対応を実測で確定する。**タイル間のサンプルずれが起動ごとに変わるか**を測る | **成功**。WNS +0.588 ns。**SMA とスライスの対応はラベルと完全に逆順**と実測で確定。**タイル内は 0.02 サンプル以内で揃い、タイル間は固定の 180°（ボードの極性反転）＋ 1〜1.5 サンプル RMS**。ゆらぎは 8 の倍数に乗らず FIFO 由来ではない。**MTS は不要と実測で裏付け** |
| [proj007](proj007/) | 2026-09-17 | 1PPS を受けて絶対時刻を貼る。エポックの付与と、PPS 基準の予約時刻（カウンタ値の一致）での取得開始 | **成功**（判定 1〜5 すべて）。**1PPS 受信・120 秒で残差 0 ビート = +0.0000 ppb**（分解能 0.0543 ppb。proj004 の CW 法 15 ppb より 2 桁以上良い）。**対照実験**: 10 MHz をスペアナ内蔵水晶に差し替えると **−111.33 ppb**（−9.62 ms/日）を検出。測定が本物であることと、ホールドオーバが基準の質に依存することを実証。+2.5 V / 100 µs でそのまま受かり、`pol=0` が真の立ち上がり、`COMP − TRIG = 58.6 ns` はオープンドレインの RC。**予約発火は 13 試行すべてで `t_start == start_at`。較正定数 `L_adc − D_pps` = 213.16 ns ± 0.09**（ケーブル入れ替えの対照実験で確定。ついでにケーブル長差 7.87 ns / 2 m = 短縮率 0.85 も実測）。**符号を逆に立てていた誤りは、当たった 10 回の測定では出ず、予言を外した対照実験で初めて出た**。rev2 で **epoch（原点の通し番号）と MMCM の locked** を flags へ出し、MMCM の源のタイルを止める対照つきの試験（`--epoch-test`）で機構を実証。**その試験を使って「較正定数はエポックごと」と決着** — `L_adc − D_pps = N(epoch) × 6.5104 ns + 5.56 ns` で、**エポック間で動くのは整数ビート N だけ（33/34/35/37）、小数部は 0.17 ns で揃う**。未知数が整数なのでエポックあたり 1 回の測定で厳密に決まる（量子化 6.51 ns がばらつきではなく固定バイアスとして出るため σ が 20 倍小さい）。WNS +0.287 ns / WHS +0.010 ns、失敗エンドポイント 0、CRITICAL WARNING なし。最悪経路は 4 本とも `clk_pl_1`（DMA 側）で、**AXIS 153.6 MHz は上位 5 に出ない**。PPS は毎秒のトリガにせず、一度合わせて以後は監視に回す（fs が整数で 1 s = 153,600,000 ビートちょうど）。`report_cdc` の Critical 97 件を読み、**実害のある 2 件（`t_age` 比較器のグリッチ）だけを修正** |
| [proj008](proj008/) | 2026-09-18 | **1PPS を ADC に常時入れ、エポックごとの較正器にする。**較正定数はエポックごとなので、値を持ち回るのをやめ、1 キャプチャ（53 µs）でその場で決める構成へ。まず 4ch 全部に同じ 1PPS を入れて較正器の特性を把握し、運用形（PPS 何本・科学 ch 何本）を決める。**RTL は proj007 rev2 と同一**で、差分は RF の配り方と PS 側のソフト層 | **進行中**（ビルド完了・実機未実施）。WNS +0.245 ns / WHS +0.010 ns、CRITICAL WARNING なし。**CDC の分類別件数 61/29/32/3 が proj007 rev2 と完全一致**し、BRAM・クロック・asynchronous 7 箇所も一致。**「RTL 同一なので WNS も +0.235 のまま」の予言は外れた**（+0.010 ns 動いた）— `MAGIC` の定数変更とプロジェクト名の変更が配置のタイブレークを動かすため。**同一性の確認には WNS ではなくCDC の分類別件数・BRAM といった構造の指紋を使う**という規約を得た | **進行中**（判定 1〜5 と時刻層の実機確認まで通過。**残りは判定 6（一晩の持続性）と 4ch への配線戻しだけ**）。WNS +0.245 ns / WHS +0.010 ns、CRITICAL WARNING なし。**RTL は proj007 rev2 と構造が完全一致**（CDC 61/29/32/3・BRAM 124.5・クロック・asynchronous 7 箇所）だが **WNS だけ +0.010 ns 動いた** — `MAGIC` の定数変更とプロジェクト名の変更が配置のタイブレークを動かすため。**同一性の確認には WNS ではなく構造の指紋（CDC の分類別件数・BRAM）を使う**という規約を得た。判定 1: 5 分配でも `PPS Clk` は受かる。判定 2: 120 秒で残差 0 ビート = **+0.0000 ppb**。**判定 3 で proj006 を別の方法で再現** — タイル内のずれを proj006 は CW トーンの位相、proj008 は PPS のエッジで測り **4〜9 ps で一致**。極性のタイル分離で proj006 の 180° を 3 つ目の独立な方法でも確認。**要求する時刻精度を 100 µs と決め（最速 1 kHz 記録の 1/10）、構成 B は「ADC に 1PPS を常時入れない・科学 4ch」に確定** — 絶対値 213 ns / Overlay ごと 6.5 ns / タイル間 1.9 ns が**全部 4〜5 桁小さい**。**proj008 ① の成果は較正器ではなく「較正が要らないと確かめたこと」**。1PPS 自体は原点・ホールドオーバ（水晶なら 15 分で予算切れ）・破綻検出のために要る。較正定数は `MAGIC` と対にして持ち、不一致なら適用を拒否する（`pynq/timebase.py`）。**実機で閉ループを確認** — 「PPS のエッジが乗るサンプル」の時刻を計算すると整数秒に戻り **−1.804 ± 0.407 ns**（予言どおり。σ はエッジのばらつき 0.336 ns と ns 丸め 0.289 ns の合成で説明できる）。原点を壊すと拒否することも実機で確認。踏んだ穴 5 件: 5 分配の反射で COMP に毎秒 1 回のグリッチ（**触っていない ADC 側にパッドを入れたら PPS Clk 側が直り、反射源が特定できた**）／**平均と RMS を比べていた**（proj006 のタイル間 1〜1.5 サンプルは起動ごとの σ であって平均オフセットではない）／**対照の操作がタイルについて非対称だった**（源のタイルだけ止めると、止めなかったタイルだけが 4 ビート動く）／複数タイルの `ShutDown()` は順序を直しても成立しない（state 14 / state 3。`overlay` で回避）／**統計的有意性を設計判断に使っていた**（要求値を含まない判定は何を測っても同じ答えを返す） |
| [proj009](proj009/) | 2026-09-18 | **fs を 4096 MSPS に上げ、IF 2〜4 GHz を第 2 ナイキストで取り、間引きなしで全レートを PL に入れる（1ch → 4ch）。**最終仕様（IF 2〜4 GHz × 4 入力、1 IF あたり 2〜8 窓、PL 内で分配）から、ハード DDC は使えず fs から見直しになった。分光計コアは proj010 以降へ送った | **成功**（1ch・4ch とも判定 1〜6）。**fs = 4096.0 MSPS**（タイル PLL R 1 / N 25 / M 3。M = 3 は初使用だが実機でロック。外部基準 −0.009 ± 0.010 ppm）。**RFDC は 1 語 16 サンプルを出せない（fs 4096 で有効値 7〜12、範囲外は黙って直前の値に戻る）** → **12 サンプル / 341.33 MHz で受け、入口のギアボックス（×4 → 非同期 FIFO → ÷3）で 16 サンプル / 256 MHz に詰め替え**。4ch で WNS +0.122 ns（−2）/ +0.122 ns（−1）。**CDC-15 が 89 + 768 × 4 = 3161 と予言どおり**（ギアボックスの FIFO がそっくり載る）。4ch で **Data_Width はタイル内の全スライスで一致が必要**（1 本ずつ変えると両方動けない）。**RF-ADC の 8 並列インタリーブのイメージ（f ± k·fs/8）は較正収束後 −53〜−66 dBc、Overlay 直後（〜5 s）は −33〜−39 dBc**。較正モード・入力周波数に依らない。外れた予言 3 件（時間スキュー支配／4ch の最悪経路は 341 MHz 側／`--probe` 直後仮説）。ch 間利得差 0.2 dB 以内 |
| [proj010](proj010/) | 2026-09-18 | **最小構成の分光計を端から端まで通す。**ADC_B 1 本・全帯域 2 GHz を 8192 点 FFT で **4096 ch × 0.5 MHz**、電力を 100 ms 積分して AXI4-Lite で読む。8192 = 16 レーン × 512 点に分解し、レーン FFT は Xilinx FFT IP × 16、ひねり係数・16 点 DFT・電力・積分（二面・デッドタイム 0）は自作 RTL。窓の切り出し（proj009 の次の一手）は、後段を共有するので後ろへ回した | **進行中**（RTL とシミュレーションまで。ビルド・実機は未実施）。`make sim`（iverilog、FFT IP はモデル）で **bit 単位・numpy・帳簿の 3 層**が SHIFT 7 / 4 とも通過。**ひねり係数の丸めは、強い線から 512 ch おきの ch に −100 dBc 級の誤差を落とす**（同じ k1 を共有するため）ことを sim で確認 |

## 凡例

- **結果**: 成功 / 失敗 / 中断 / 保留。失敗した proj も消さない。何が駄目だったかが資産
- 番号は「試したことの単位」で増やす。日付ではない。1日に複数増えてよい
